# Netlify Environment Variables
# These are automatically set by Netlify during build

PUBLIC_URL=/
REACT_APP_BACKEND_URL=/.netlify/functions
NODE_VERSION=18

# Optional: Enable production optimizations
NODE_ENV=production
GENERATE_SOURCEMAP=false
